# Recent Agent Work Log

Append entries in chronological order so the oldest entry stays at the top. Use:

```text
## [YYYY-MM-DD] kind | concise title
```

Keep each entry evidence-backed and concise. Run the archive helper when configured budgets are exceeded.
