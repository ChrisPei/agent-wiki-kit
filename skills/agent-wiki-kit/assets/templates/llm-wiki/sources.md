# Source Register

Register evidence stored under `raw/`, including its origin, authority, capture date, and lifecycle policy. Add a new row or version when adding or intentionally changing a source; never silently rewrite an artifact to match a later conclusion.

| Source | Added | Origin | Authority | Notes |
| --- | --- | --- | --- | --- |
| _Add source path_ | YYYY-MM-DD | human / external / import | primary / supporting | _Acquisition context_ |
